name: Folder5 CI

on:
  workflow_dispatch:

jobs:
  run-folder5:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run folder5.py
        run: python Folder5/folder5.py
